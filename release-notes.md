## 🐛 Bug 修复

- **项目启动失败**：修复 `start_project` 用错误 ID 匹配服务导致项目启动静默失败的问题
- **进程泄漏**：修复 `start_service` 测试阶段超时后产生孤儿进程，同一服务可能同时运行两个实例
- **批量停止不完整**：修复 `batch_stop_services` 不清理手动检测到的进程
- **大文件 OOM**：修复日志读取将整个文件加载到内存的问题，改为按需读取末尾 N 行
- **锁阻塞 UI**：修复读取日志期间持有锁导致前端轮询阻塞、UI 闪现未响应

## ⚡ 性能优化

- **启动速度**：移除同步测试阶段，服务启动从 ~10 秒卡顿优化到 ~150ms
- **渲染性能**：项目数量计算优化为预计算 O(1) 查询
- **日志读取**：大文件使用末尾按块读取策略

## ✨ 改进

- 错误提示 5 秒后自动消失
- 命令终端输出限制 5000 行上限
- 环境配置精简为仅 JAVA_HOME
- 移除未使用的依赖和死代码
- 新增技术说明书

## 📦 安装

下载 `Service Deck_1.0.0_x64-setup.exe` 双击安装即可。
